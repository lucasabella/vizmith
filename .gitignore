__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.*
!.env.example

node_modules/
dist/
build/

*.duckdb
*.duckdb.wal
data/
!tests/fixtures/data/

# `vizmith eval` writes here by default, and a run record names the endpoint it asked.
# That is somebody's internal URL on a machine that is not theirs to publish.
eval-runs/

.claude/

.DS_Store
.idea/
.vscode/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
coverage/

CLAUDE.md

# Written by CI from uv.lock on the way in, so the install can be hash checked. The lock is
# what is committed; this is the export of it and would be one more thing to keep in step.
/requirements.txt
